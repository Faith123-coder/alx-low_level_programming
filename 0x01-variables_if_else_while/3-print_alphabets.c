#include <stdio.h>
/**
 * main - Print the alphabet in lowercase then in uppercase
 * Return: 0 (Sucess)
 */
int main(void)
{
	int n = 97;
	int m = 65;

	while (n <= 122)
	{
		putchar(n);
		n++;
	}
	while (m <= 90)
	{
		putchar(m);
		m++;
	}
	putchar('\n');
	return (0);
}
